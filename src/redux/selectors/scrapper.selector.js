
// feature selectors

export const getUrlsScrapperSelector = state => state.scrapper.urls
export const getAllScrapperSelector = state => state.scrapper.all

// query selectors

// memoized selectors
