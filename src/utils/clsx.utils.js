/**
 * Returns classes separated by blank space and in a string
 * @param {Array} stylesArr - array of styles
 * @returns {String}
 */
const clsx = (stylesArr) => stylesArr.join(' ')

export default clsx
